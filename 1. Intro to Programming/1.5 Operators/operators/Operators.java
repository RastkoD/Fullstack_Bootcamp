package operators;

public class Operators {

	public static void main(String[] args) {
		/*
		double volume;
		double r;
		double h;
		
		r = 8;
		h = 13;
		
		volume = (1.0 / 3.0) * r * r * h * Math.PI;
		System.out.println("For radius: " + r + " and height: " + h + ", cone volume is: " + volume);
		*/
		
		/*
		double l = 5;
		double RCu = 1.588e-8;
		double R;
		double d = 2;
		
		
		R = (RCu * 4 * l) / (d * d * Math.PI);
		System.out.println("Resistance of the copper conductor: " + R + " ohms");
		*/
		
		/*
		double r;
		double H;
		double area;
		
		r = 4;
		H = 8;
		
		area = 2 * r * (r + H) * Math.PI;
		System.out.println("Cylinder area if r: " + r + " and H: " + H + " is: " + area);
		*/
	}

}
